{
  "name": "@pi-web/bridge",
  "version": "0.3.0",
  "private": true,
  "description": "WebSocket RPC bridge server for Pi Web",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./*.ts",
      "default": "./*.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.74.0",
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "engines": {
    "node": ">=20.6.0"
  }
}
